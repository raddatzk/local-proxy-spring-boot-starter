group=me.raddatz

# Only the major is declared here. The release workflow appends the number of
# commits on master, so every new state of master is a new, higher version:
# <major>.<commits>. A local build is a snapshot of the current major.
majorVersion=0

org.gradle.parallel=true
org.gradle.caching=true
